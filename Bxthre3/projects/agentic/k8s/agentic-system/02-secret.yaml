apiVersion: v1
kind: Secret
metadata:
  name: agentic-secrets
  namespace: agentic
type: Opaque
stringData:
  # Add via: kubectl create secret generic agentic-secrets \
  #   --from-literal=AGENTIC_API_KEY=your-key \
  #   --from-literal=OLLAMA_API_KEY=your-key \
  #   -n agentic
  AGENTIC_API_KEY: "REPLACE_ME"
