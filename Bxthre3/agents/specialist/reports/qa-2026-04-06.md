# QA Report — AgentOS
**Date:** 2026-04-06
**QA Lead:** QA-Lead Agent
**Runtime:** 20:05 UTC
**Status:** ⚠️ DEGRADED — Release-blocking test failure detected

---

## Executive Summary

| Suite | Result | Change vs Yesterday |
|-------|--------|---------------------|
| MCP-Mesh (bun) | 🔴 12/13 FAIL | 5/5 → 12/13 (+1 new failure) |
| Android APK | ✅ APK builds | No change |
| Backend API | ✅ OPERATIONAL | Was DOWN, now resolved |
| Cross-Platform Integration | ⚠️ SKELETON | No change |
| Flaky Tests | ⚠️ 1 identified | New flaky test found |

**🔴 P1 ESCALATION REQUIRED** — Deduplication test in mesh-sync is failing.

---

## 1. MCP-Mesh Test Suite (bun test)

**Location:** `Bxthre3/projects/the-agentos-project/mcp-mesh/`

```
bun test v1.2.21 (7c45ed97)

tests/mesh.test.ts:
(pass) MCP Mesh > should parse CLI args correctly [0.09ms]
(pass) MCP Mesh > should validate message schema [0.04ms]
(pass) MCP Mesh > should create mesh config [0.07ms]
(pass) Peer Registry > should track peer states [0.05ms]
(pass) Heartbeat > should calculate heartbeat interval [0.02ms]

tests/mesh-sync.test.ts:
(pass) Mesh Event Propagation > broadcast reaches all connected nodes [0.50ms]
(pass) Mesh Event Propagation > state sync propagates across chain [0.53ms]
(fail) Mesh Event Propagation > duplicate delivery is rejected (at-least-once semantics) [0.40ms]
(pass) State Sync Conflict Resolution > concurrent writes resolve by timestamp [0.11ms]
(pass) State Sync Conflict Resolution > vector clock tracks causality [0.18ms]
(pass) State Sync Conflict Resolution > state export/import roundtrip [0.07ms]
(pass) Cross-Platform Transport Simulation > WebSocket transport would handle reconnection [0.06ms]
(pass) Cross-Platform Transport Simulation > SSE transport deduplication works [0.07ms]

  12 pass | 1 fail
  Ran 13 tests across 2 files. [30.00ms]
```

### Failing Test Detail

**Test:** `duplicate delivery is rejected (at-least-once semantics)`
**File:** `tests/mesh-sync.test.ts:122`
**Error:**
```
error: expect(received).toBe(expected)
Expected: 1
Received: 2
```
**Root Cause:** Deduplication logic not properly tracking duplicate broadcasts. The test sends the same event ID twice (`dedup_test`) from `node-A` to `node-B`, expecting `node-B.receivedEvents.length` to remain unchanged after the second broadcast. Instead, the count increments to 2, indicating the dedup window or dedup key logic is not functioning correctly.

**Failure Rate:** 100% (fails every run — not flaky in that sense, but the test itself may be incorrectly written)

### Test Metrics

| Metric | Value |
|--------|-------|
| Pass Rate | 92.3% (12/13) |
| Flaky Tests | 1 (the dedup rejection test) |
| Test Duration | 30ms |
| Total expect() calls | 24 |

**Verdict:** Core mesh logic functional. Deduplication semantics need review — either implementation bug or test expectation mismatch.

---

## 2. Android Test Suite

**Location:** `Bxthre3/projects/the-agentos-project/android/` (source: `Bxthre3/projects/agentic/the-agentos-project/client/android/`)

**Source:** `android-2026-04-06.md` (Android-Lead report)

| Check | Result | Notes |
|-------|--------|-------|
| `./gradlew assembleDebug` | ✅ BUILD SUCCESSFUL | 33s, 34 tasks |
| APK | ✅ Generated | 24MB at correct path |
| Compile Errors | ✅ None | |
| Kotlin Warnings | ⚠️ 1 | `apiClient` param unused in `DashboardTab()` |
| SDK Location | ✅ Fixed | `local.properties` created |

### API Endpoint Assessment (from Android-Lead report)

| Endpoint | Expected Path | Actual Path | Status |
|-----------|---------------|-------------|--------|
| Health | `GET /api/agentos/health` | `GET /api/health` | ❌ 404 |
| Tasks | `GET /api/agentos/tasks` | Mapped via status | ✅ |
| Agents | `GET /api/agentos/agents` | Mapped via status | ✅ |
| Roadmap | `POST /v1/roadmaps` | Non-existent | ❌ 404 |

**Note:** Android APK is building successfully but all 7 tabs are placeholder screens. Ktor WebSocket dependency is present but unused. MCP mesh client not implemented.

### No instrumented Android tests found

No `.kt` or `.java` test files exist in `android/app/src/test/`. The Android suite relies on APK build validation and manual screen review. Full `./gradlew connectedAndroidTest` requires Android SDK/emulator not available in container.

**Verdict:** APK builds successfully. No automated unit/instrumentation tests present.

---

## 3. Cross-Platform Integration Tests

**Location:** `Bxthre3/projects/the-agentos-project/testing/integration/`

**Skeleton only** — requires running mesh server. No live integration tests can execute in the container environment.

**Backend API Health:** ✅ OPERATIONAL
```
GET https://brodiblanco.zo.space/api/agentos/status
Response: 200 OK
version: 6.0.0
status: operational
agentCount: 19
activeAgents: 16
workQueueDepth: 12
escalationCount: 14
avgHealth: 0.91
```

**Verdict:** Backend incident from 2026-04-05 is **RESOLVED**. The P0 has been cleared.

---

## 4. Flaky Tests

| Test | Failure Rate | Pattern | Analysis |
|------|-------------|---------|----------|
| `duplicate delivery is rejected` | 100% (deterministic fail) | Always fails | Either: (a) dedup implementation is broken, or (b) test sends two events with same ID but they are being treated as distinct. Needs mesh-engineer review. |

**Note:** No timing-based flakiness observed. The test failure is consistent and deterministic.

---

## 5. Regression Check

### Resolved Since Yesterday
- ✅ **Backend P0 INCIDENT RESOLVED** — `/api/agentos/*` routes operational (was DOWN on 2026-04-05)

### Still Open (Carried Forward)
- P2: Android API base URL wrong (`zo.computer` vs `zo.space`)
- P2: Android `/api/agentos/` path prefix missing
- P2: All 7 Android tabs are placeholders
- P2: MCP WebSocket client not implemented in Android
- P3: `generateRoadmap()` targets non-existent `/v1/roadmaps`
- P3: Kotlin unused parameter warning in `DashboardTab`

### No New Regressions Detected
MCP-mesh core logic (12/12 other tests) remains stable. No regressions in previously passing tests.

---

## 6. 🔴 P1 Escalation — Release-Blocking Test Failure

**[QA] AgentOS release-blocking test failure**

**Test:** `duplicate delivery is rejected (at-least-once semantics)`
**File:** `tests/mesh-sync.test.ts:122`
**Severity:** Release-blocking — deduplication is a core mesh protocol guarantee

**Impact:** If deduplication is not working, the mesh could deliver duplicate events to peers, corrupting state and violating at-least-once delivery semantics specified in the protocol.

**Owner:** Mesh-Engineer

**Action Required:**
1. Determine if this is an implementation bug (dedup not working) or test bug (test expectation wrong)
2. If implementation bug: fix the dedup window/key logic in `transport/sse.ts` or `core/state-sync.ts`
3. If test bug: correct the test to match the actual deduplication semantics

---

## 7. Known Issues

| Issue | Severity | Status | Resolution |
|-------|----------|--------|-----------|
| MCP-mesh deduplication test failing | P1 🔴 | OPEN | Mesh-Engineer review required |
| Android API base URL wrong | P2 ⚠️ | Open | Dev action |
| Android path prefix missing | P2 ⚠️ | Open | Dev action |
| All 7 Android tabs placeholders | P2 ⚠️ | Open | Dev action |
| MCP WebSocket client not in Android | P2 ⚠️ | Open | Mesh-Engineer/Dev |
| Desktop tests not implemented | P3 | Pending | ETA: Q2 Week 2 |
| Cross-platform integration skeleton | P3 | Pending | Requires mesh server |

---

## 8. Release Readiness

| Platform | Status | Blocker? |
|----------|--------|----------|
| MCP-Mesh (bun) | 🔴 12/13 | **YES** — P1 dedup test failing |
| Android APK | ✅ BUILD OK | No (but no unit tests) |
| Backend API | ✅ OPERATIONAL | No |
| zo.space API | ✅ OPERATIONAL | No |
| Desktop JAR | ⚠️ PARTIAL | No (E2E not complete) |

**Overall: 🔴 NOT RELEASE-READY** — P1 dedup test failure is a release-blocking defect in the mesh protocol.

---

## 9. Recommendations

### Immediate (P1 — Block Release)
1. **Mesh-Engineer must resolve** the deduplication test failure before any release
2. Determine whether the dedup implementation or the test expectation is wrong

### Short-term (P2)
1. Fix Android API base URL and path prefix
2. Implement at least one real Android screen (dashboard with live data)
3. Add MCP WebSocket client to Android

### Medium-term (P3)
1. Implement desktop E2E tests
2. Add instrumented Android unit tests
3. Complete cross-platform integration test suite

---

## Appendix: Backend Status Detail

**API:** `https://brodiblanco.zo.space/api/agentos/status`
**Response:** 200 OK
**Payload:** Full org status v6.0.0
- 19 agents (16 active, 3 idle)
- 15 tasks (9 in-progress, 6 done)
- 9 integrations (8 connected, 1 partial)
- avgHealth: 0.91

**Previously reported P0 (agentos-api DOWN) is resolved.**

---

*Report generated: 2026-04-06 20:05 UTC*
*Next scheduled run: 2026-04-07 14:00 UTC*
*QA-Lead | Bxthre3/AgentOS*
